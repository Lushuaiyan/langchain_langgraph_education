# agent记忆功能
在langchain中没有专门的工具能够自动管理agent的记忆
需要用户自己实现这项功能
> 在langgraph中有这个功能, 后续再介绍

## 1. 内存保存
直接维护一个`messages`列表, 每次对话手动添加记录

> 优点: 存在内存中, 调用很快
> 缺点: 无法实现多个对话, 没有长期记忆

## 2. 文件/数据库保存
将对话内容保存成文件或保存到数据库中, 可以有多个会话

> 优点: 具有多个会话, 能够长期保存, 可以跨会话使用(方便传递)
> 缺点: 导入速度较慢